apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: loki-config
spec:
  refreshInterval: 10m
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  dataFrom:
    - extract:
        key: monitoring-loki-config
