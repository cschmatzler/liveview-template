apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: tempo
  namespace: monitoring
spec:
  interval: 10m
  chart:
    spec:
      sourceRef:
        kind: HelmRepository
        name: grafana
        namespace: flux-system
      chart: tempo-distributed
      version: 1.3.0
  maxHistory: 2
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  valuesFrom:
    - kind: ConfigMap
      name: tempo-values
    - targetPath: storage.trace.s3.endpoint
      kind: Secret
      name: tempo-config
      valuesKey: BUCKET_ENDPOINT
    - targetPath: storage.trace.s3.region
      kind: Secret
      name: tempo-config
      valuesKey: BUCKET_REGION
    - targetPath: storage.trace.s3.access_key_id
      kind: Secret
      name: tempo-config
      valuesKey: AWS_ACCESS_KEY_ID
    - targetPath: storage.trace.s3.secret_access_key
      kind: Secret
      name: tempo-config
      valuesKey: AWS_SECRET_ACCESS_KEY
