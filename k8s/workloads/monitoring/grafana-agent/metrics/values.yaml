controller:
  type: deployment
  replicas: 2
agent:
  configMap:
    create: false
    name: grafana-agent-metrics-config
    key: config.river
  resources:
    requests:
      cpu: 128m
      memory: 1024M
    limits:
      memory: 1024M
configReloader:
  enabled: false
