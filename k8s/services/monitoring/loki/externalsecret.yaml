apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: loki-bucket
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  dataFrom:
    - extract:
        key: monitoring-loki-bucket
