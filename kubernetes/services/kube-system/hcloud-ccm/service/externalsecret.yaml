apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: hcloud-test
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  data:
    - secretKey: token
      remoteRef:
        key: hcloud
        property: token
