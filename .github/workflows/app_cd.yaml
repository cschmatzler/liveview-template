name: Deploy app
on:
  push:
    branches:
      - master
    paths:
      - template

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

jobs:
  build:
    name: Build production image
    runs-on: buildjet-4vcpu-ubuntu-2204-arm
    steps:
      - name: Checkout
        uses: actions/checkout@8e5e7e5ab8b370d6c329ec480221332ada57f0ab # v3

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@4b4e9c3e2d4531116a6f8ba8e71fc6e2cb6e6c8c # v2

      - name: Log into GHCR
        uses: docker/login-action@f4ef78c080cd8ba55a85445d5b36e214a81df20a # v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set environment
        run: echo "VERSION=$(date '+%y.%-m.%-d')-${GITHUB_SHA::7}" >> $GITHUB_ENV

      - name: Update version file
        run: echo $VERSION > version

      - name: Build and push image
        uses: docker/build-push-action@3b5e8027fcad23fda98b2e3ac259d8d67585f671 # v4
        with:
          context: template
          tags: ghcr.io/cschmatzler/liveview-template:latest,ghcr.io/cschmatzler/liveview-template:${{ env.VERSION }}
          push: true
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # release:
  #   name: Release
  #   runs-on: ubuntu-latest
  #   needs: build
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@8e5e7e5ab8b370d6c329ec480221332ada57f0ab # v3
  #
  #     - name: Set environment
  #       run: echo "VERSION=$(date '+%y.%-m.%-d')-${GITHUB_SHA::7}" >> $GITHUB_ENV
  #
  #     - name: Update version file
  #       run: echo $VERSION > version
  #
  #     - name: Push new version and tag
  #       run: |
  #         git config --global user.name "release[bot]"
  #         git config --global user.email "actions@liveview-template.app"
  #         git commit -am "Release $VERSION"
  #         git push
  #         git tag -a $VERSION -m "Release $VERSION"
  #         git push --tags
